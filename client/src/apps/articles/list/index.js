import React from 'react';
import PageContainer from '../../../components/page-container';

const List = ({classes, ...props}) => {
    return (
        <PageContainer>
        </PageContainer>
    )
}

export default List;